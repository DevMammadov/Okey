import React from "react";
import { Grid } from "@material-ui/core";

export const Header = () => {
  return (
    <Grid container>
      <Grid item xs={12}>
        header
      </Grid>
    </Grid>
  );
};
