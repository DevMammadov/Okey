import React, { FC, useState } from "react";
import { Grid, Collapse, Icon } from "@material-ui/core";

interface IPros {}

export const Menu: FC<IPros> = () => {
  return (
    <Grid container>
      <Grid item> Dssmos </Grid>
    </Grid>
  );
};
